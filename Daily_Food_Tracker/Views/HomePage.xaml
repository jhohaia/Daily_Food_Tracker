<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:Daily_Food_Tracker.ViewModels"
             x:Class="Daily_Food_Tracker.Views.HomePage"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:HomePageViewModel />
    </ContentPage.BindingContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">
                <Label Text="Welcome, User" FontSize="Title"/>
                <Label Text="Your daily calory goal progress:" FontSize="16" Padding="0,0,0,0" />
                <ProgressBar Progress="0.5" />
                <Label FontSize="16" Padding="0,24,0,0" Text="Breakfast +"/>
                <Label FontSize="16" Padding="0,24,0,0" Text="Lunch +"/>
                <Label FontSize="16" Padding="0,24,0,0" Text="Dinner +"/>
                <Label FontSize="16" Padding="0,24,0,0" Text="Snacks +"/>
            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>